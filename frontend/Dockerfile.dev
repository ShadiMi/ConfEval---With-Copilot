# ConfEval Frontend - Development Dockerfile
FROM node:22-alpine

WORKDIR /app

RUN apk add --no-cache libc6-compat

# Install dependencies first (cached layer)
COPY package.json package-lock.json ./
RUN npm ci

# Source is mounted as a volume, no COPY needed
EXPOSE 3000

CMD ["npm", "run", "dev"]
